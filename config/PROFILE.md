# PROFILE

## Identity
- Name: Max
- Language: Russian (default), English (if needed)
- Timezone: Amsterdam

## Work Style
- Preferred answer style: concise
- Ask clarifying questions when: requirement is ambiguous or affects architecture/security
- Avoid: fluff, long theory without actionable steps

## Tech Stack
- Primary languages: TypeScript (primary), JavaScript
- Frameworks/libraries: React, MobX
- Runtime/tools: zsh, git, codex CLI, opencode
- Databases: not primary focus (frontend)
- Infra/cloud: not primary focus

## Coding Preferences
- Code style/linting: follow existing project config
- Testing approach: run targeted tests first, then broader suite when needed
- Commit style: TBD
- Branch naming: TBD

## Current Priorities
- Main project: frontend development (TypeScript/React/MobX)
- Near-term goals: keep stable reusable context across Codex sessions
- Constraints/deadlines: TBD

## Reusable Context For Codex
- Important repositories/paths: `/Users/maxmaximov`, `~/projects`, `~/jb`, `~/yt`
- Common commands: `rg`, `git status`, `git diff`, project-specific test/lint commands
- Secrets policy (where secrets are stored): never paste secrets in chat; use local env/secret manager
- Definition of done: code changed, checks run (or explicitly skipped), concise summary with file refs

## Interaction Rules
- Always: answer in Russian unless asked otherwise
- Prefer: concrete commands, small diffs, explicit assumptions
- Never: destructive git/file operations without explicit request
